---
# Config file for Trivy.
exit-code: 1

format: table

pkg:
  type:
    - os
    - library

scan:
  skip-files:
    - usr/local/bin/prometheus
    - usr/local/bin/promtool

severity:
  - MEDIUM
  - CRITICAL
  - HIGH

scanners:
  - vuln

vulnerability:
  ignore-unfixed: true
